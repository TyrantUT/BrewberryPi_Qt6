### This file is automatically generated by Qt Design Studio.
### Do not change

qt_add_library(BrewberryPi STATIC)
set_source_files_properties(Constants.qml
    PROPERTIES
        QT_QML_SINGLETON_TYPE true
)

set_source_files_properties(BreweryValues.qml
    PROPERTIES
        QT_QML_SINGLETON_TYPE true
)

# Detect if the target architecture is ARM
if (PLATFORM_ARM)
    message(STATUS "Including pigpio related directories and libraries")
    set(PIGPIO_SOURCES "")
    target_include_directories(BrewberryPi PRIVATE /sysroot/usr /sysroot/usr/include)
    target_link_directories(BrewberryPi PRIVATE /sysroot/usr/lib)
else()    
    message(STATUS "Skipping pigpio files.")
    set(PIGPIO_SOURCES pigpio.h pigpio.cpp)
endif()

qt6_add_qml_module(BrewberryPi
    URI "BrewberryPi"
    VERSION 1.0
    QML_FILES
        Constants.qml
        BreweryValues.qml
        BreweryFunctions.js
    SOURCES
        max31865.h
        max31865.cpp
        rpihelper.h
        rpihelper.cpp
        pidcontroller.h
        pidcontroller.cpp
        rpithreads.h
        rpithreads.cpp
        rpidata.h
        rpidata.cpp
        connectionmanager.h
        connectionmanager.cpp
        ${PIGPIO_SOURCES}
)
